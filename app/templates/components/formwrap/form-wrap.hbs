{{#unless noLabelWrap}}
  {{#if label}}
    {{em-label text=label viewName=labelViewName}}
  {{/if}}
{{/unless}}
{{#if controlWrapClass}}
  <label {{bind-attr class=controlWrapClass}}>
    {{#if yieldLabel}}
      {{view controlView viewName=controlViewName property=propertyName}}
      {{label}}
    {{/if}}
  </label>
{{else}}
  {{view controlView viewName=controlViewName property=propertyName}}
{{/if}}
{{#if needValidation}}
  {{#if hasError}}
    <span class="help-block">{{errorMsg}}</span>
  {{/if}}
{{/if}}
